# ----------------------------------------------------------------------------------------------------------------------
# 预加载核心配置
# ----------------------------------------------------------------------------------------------------------------------
spring:
  application:
    name: "node-configuration"
  profiles:
    active: dev
    include: default,cnf

---

spring:
  profiles: dev

---

spring:
  profiles: docker
