# ----------------------------------------------------------------------------------------------------------------------
# 核心配置
# ----------------------------------------------------------------------------------------------------------------------
spring:
  cloud:
    config:
      allow-override: true
      server:
        git:
          uri: "git@github.com:yingzhuo/spring-cloud-examples-configuration.git"
          clone-on-start: true
