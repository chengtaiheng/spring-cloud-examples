# ----------------------------------------------------------------------------------------------------------------------
# 预加载核心配置
# ----------------------------------------------------------------------------------------------------------------------
spring:
  application:
    name: "node-apigateway"
  profiles:
    active: dev
    include: default,route

---

spring:
  profiles: dev

---

spring:
  profiles: docker
